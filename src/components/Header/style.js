var React = require('react-native');

var view = React.StyleSheet.create({
    header: {
        padding: 10,
        marginTop: 20,
        backgroundColor: "#707070"
    },
  });

  module.exports = view;